import dotenv from 'dotenv';
dotenv.config();

console.log('ACCESS_DB_PATH_COMPRAS:', process.env.ACCESS_DB_PATH_COMPRAS);
console.log('ACCESS_DB_PATH:', process.env.ACCESS_DB_PATH);
console.log('NODE_ENV:', process.env.NODE_ENV);
